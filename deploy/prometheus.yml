global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: thermalos
    static_configs:
      - targets: ['thermalos:9101']
    relabel_configs:
      - source_labels: [__address__]
        target_label: instance
